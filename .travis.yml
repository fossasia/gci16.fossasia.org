language: ruby
services: docker
rvm:
  - 2.3.3
install:
  - bundle install
script:
  - bundle exec jekyll build
  - docker run --volume=$(pwd):/app --workdir=/app coala/base coala -C
branches:
  only:
    - gh-pages
  
